export interface Product {
  id: string;
  collectionId: string;
  collectionName: string;
  description: string;
  in_stock: boolean;
  price: number;
  title: string;
  created: string;
  updated: string;
}
